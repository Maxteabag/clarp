---
name: clarp-directories
description: Publish a safe directory shortcut that opens in Clarp's file explorer.
---
# Directories
Use `clarp-agent-artifacts create "$CLAUDE_PWA_SESSION" directory TITLE SUMMARY JSON_PAYLOAD` with `root` (`workspace` or `home`) and a safe relative path. Never expose an absolute host path or traversal.
