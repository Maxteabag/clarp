---
name: clarp-releases
description: Publish a verified software release.
---
# Releases
Publish with `clarp-agent-artifacts create "$CLAUDE_PWA_SESSION" release TITLE SUMMARY JSON_PAYLOAD`.
Create type `release` with `version` and relevant `build`, `commit`, `repository`, `source_url`, and verification `content`.
